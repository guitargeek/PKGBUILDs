--- pyfiles/tabletex.py	2019-10-25 21:33:24.406562564 +0200
+++ pyfiles/tabletex.py	2019-10-25 21:32:34.483229634 +0200
@@ -1,8 +1,13 @@
 #!/usr/bin/python
 
-import math, sys, os, commands
+import math, sys, os, subprocess
 from itertools import groupby
 
+def get_status_output(*args, **kwargs):
+    p = subprocess.Popen(*args, **kwargs)
+    stdout, stderr = p.communicate()
+    return p.returncode, stdout, stderr
+
 beginStr = """
 \\documentclass{article}
 \\usepackage{multirow}
@@ -125,14 +130,14 @@
     fh.write(content)
     fh.close()
 
-    status,out = commands.getstatusoutput("pdflatex -interaction=nonstopmode -output-directory=%s %s" % (basedir, basename+".tex"))
+    status,out,_ = get_status_output("pdflatex -interaction=nonstopmode -output-directory=%s %s" % (basedir,basename+".tex"), stderr=subprocess.STDOUT)
     # print out
     if(" Error" in out):
         print "[TM] ERROR: Tried to compile, but failed. Last few lines of printout below."
         print "_"*40
         print "\n".join(out.split("\n")[-30:])
     else:
-        status,out = commands.getstatusoutput("pdfcrop %s %s" % (basename+".pdf", basename+".pdf"))
+        status,out,_ = get_status_output("pdfcrop %s %s" % (basename+".pdf", basename+".pdf"), stderr=subprocess.STDOUT)
         print "[TM] Created %s" % (basename+".pdf")
 
 def getString(fname, complete=True):
